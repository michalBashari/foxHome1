import { MyDairectiveDirective } from './my-dairective.directive';

describe('MyDairectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDairectiveDirective();
    expect(directive).toBeTruthy();
  });
});
